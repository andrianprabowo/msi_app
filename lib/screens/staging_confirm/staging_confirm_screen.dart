import 'package:flutter/material.dart';

class StagingConfirmScreen extends StatelessWidget {
  static const routeName = '/staging_confirm';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center());
  }
}
