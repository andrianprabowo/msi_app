import 'package:flutter/material.dart';

class PickerCheckScreen extends StatelessWidget {
  static const routeName = '/picker_check';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Pick Check')),
    );
  }
}
